# 2026-05-16

## 网站全局：添加右侧悬浮微信/WhatsApp联系方式组件

将所有页面的"预约看艇"和"邮件咨询"按钮替换为固定在页面右侧中间的微信和WhatsApp图标联系组件。

**改动内容：**
1. **styles.css** - 替换旧的 `.floating-cta` 样式为新的 `.sidebar-contact` 样式，定位方式从右下角改为右侧垂直居中（`top: 50%; transform: translateY(-50%)`），带模糊背景和悬浮信息展开效果
2. **index.html** - 替换旧的 floating-cta 为 sidebar-contact
3. **其他 11 个页面** - 新增 sidebar-contact 组件（about, buying-process, circumnavigation, contact, customization, knowledge, longrange, polar, portfolio, regional, research）
4. **blog.html, buy.html, rent.html** - 补充加载 `styles.css`（之前只加载了 `style.css`）

**侧边栏特性：**
- 位于页面右侧中间，`position: fixed` 跟随滚动
- 两个图标：WhatsApp（点击跳转 wa.me/+971561018837）和微信（点击弹窗显示 ID）
- 鼠标悬停时展开显示详细联系信息
- 移动端适配：底部横向排列
- 联系信息：Wechat ID: Joeybaojunjie / Whatsapp: +971561018837

## 清理：删除三个无关页面

**删除文件：** blog.html, buy.html, rent.html

**原因：** 这三个页面属于另一个项目——"迪拜新游艇 New Yachting LLC"（通用游艇中介平台），与当前"探险游艇 Explorer Yacht"网站无任何关联。

**判断依据：**
- 品牌显示为"迪拜新游艇"，非"探险游艇"
- 独立的导航结构，主站无任何页面链接到它们
- JSON-LD 引用的是 xinyouting.com 域名
- 使用独立的 style.css（非主站 styles.css）

## P0 修复：重复 `<title>` 标签 (7个页面)

移除了以下页面中 SEO 标签后的第二个重复 `<title>` 标签：
- buying-process.html（保留：探险游艇购买流程 | 迪拜·香港游艇购买指南）
- circumnavigation.html（保留：环球探险游艇 | 迪拜·香港环球航行游艇定制）
- knowledge.html（保留：探险游艇知识库 | 购买指南·设计理念·航海知识）
- longrange.html（保留：远航探险游艇 | 跨洋长航程探险游艇）
- polar.html（保留：极地探险游艇 | 破冰探险游艇定制）
- regional.html（保留：短距探险游艇 | 区域性探险游艇定制）
- research.html（保留：科考探险游艇 | 海洋研究探险游艇定制）

## P0 修复：提取重复内联 CSS 到公共文件

**问题：** 7个页面（polar, research, regional, portfolio, knowledge, contact, about）各包含 ~370-420 行完全相同的"统一导航和页脚样式"内联 CSS，造成约 2800 行重复代码。

**修复：**
1. **styles.css** — 新增页脚样式块（~160行），包含 .footer-main/.footer-container/.footer-brand/.footer-links/.footer-contact/.footer-bottom/admin-link/privacy-link/separator 等选择器及响应式断点
2. **series.css** — 新增"Unified Explorer 导航覆写"（~30行），隐藏 .top-bar 并将 .main-nav 置顶为毛玻璃半透明样式
3. **portfolio.css** — 新增同样的导航覆写（portfolio.html 加载 portfolio.css 而非 series.css）
4. **移除** — 7个页面的"统一导航和页脚样式"内联块已全部删除，页面特定样式块完整保留

## P1 残留页脚 CSS 清理（Phase 3）

**问题：** Phase 2 只捕获了带"统一导航和页脚样式"注释的内联块，但另有 8 个页面包含完全相同但没有该注释的页脚 CSS 残留。

**已完成：**
1. **about.html** — 移除残留页脚 `<style>` 块
2. **knowledge.html** — 移除残留页脚 `<style>` 块
3. **polar.html** — 移除残留页脚 `<style>` 块
4. **regional.html** — 移除残留页脚 `<style>` 块
5. **research.html** — 移除残留页脚 `<style>` 块
6. **circumnavigation.html** — 移除残留页脚 `<style>` 块
7. **portfolio.html** — 移除残留页脚 `<style>` 块
8. **footer.html** — 移除残留页脚 `<style>` 块
9. **contact.html** — 内联块全为页脚CSS（~230行），完整移除
10. **customization.html** — 混合块：视频播放/弹窗/占位符样式（~280行）提取到 customization.css，页脚部分移除，内联块完全删除
11. **index.html** — 3个内联块：计算器样式（620行）+ 问卷样式（37行）提取到新建的 index.css（662行），页脚块（219行）删除。index.css 已链接到 `<head>`

**新建文件：**
- `index.css`（662行）—— 成本计算器 + 问卷链接区域页面专用样式

**修改文件：**
- `customization.css` — 追加视频播放、弹窗、展示区、占位符和备用内容样式（~250行）

**成果：**
- 11 个 HTML 文件内联 `<style>` 块归零（contact, customization, index 及之前的 8 个）
- 累计消除 ~4400 行重复内联 CSS（P0 ~2800 + P1 ~1600）
- 页面特定样式全部集中到独立 CSS 文件（styles.css / series.css / portfolio.css / customization.css / index.css）
- 剩余带内联块的 4 个文件（about/admin-login/admin/buying-process）均为页面特有的非页脚样式，无需处理

## 首页文案更新

Hero 区域文字调整：
- H1: "高端探险游艇定制与远航服务" → "分享探险和远航游艇知识"
- Subtitle: "服务迪拜、香港、深圳、海南及全球华人船东…" → "立足于阿联酋迪拜、服务遍及中东、粤港澳大湾区及全球船东，覆盖环球航行、极地探险、科考远航与船队运营管理。"
